# Response DTOs

Provides generic HTTP response structures shared by handlers. `HttpResponse` wraps the raw payload for tests, while `BaseResponse` and `GenericResponse` model standard JSON responses.
