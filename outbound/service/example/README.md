# Example Service Outbound

This package demonstrates a typical outbound integration. The `Service`
aggregator builds HTTP, gRPC and Kafka clients for the Example service and caches
instances for reuse. Tests show how these clients can be stubbed during unit
and integration tests.
