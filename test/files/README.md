# File Handler Tests (`test/files`)

Integration tests covering multipart uploads, downloads and form submissions handled by the REST server. They spin up a temporary server instance and verify responses.
