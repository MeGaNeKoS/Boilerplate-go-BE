# Item Repository (`repositories/item`)

Implements CRUD operations for `Item` models using the SQL helpers in `infrastructure/db`. The repository is retrieved via the aggregator and used by the example service.
