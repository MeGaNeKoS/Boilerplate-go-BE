# Item Service (`services/item`)

Implements CRUD-style business logic for managing items. The service uses the item repository for data access and optionally calls the Example outbound service during updates. Tests in this folder verify the logic without any transport layer.
