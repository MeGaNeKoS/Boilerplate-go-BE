# Test Utilities (`test/testutil`)

Common helpers for integration tests. Provides a `StubLogger`, functions to patch database access and monkey patch the logger, as well as helpers to generate temporary RSA key files. Using these utilities keeps the tests focused on behaviour instead of setup.
