# Response DTOs

Provides generic HTTP response structures shared by handlers. `HttpResponse` wraps the raw payload for tests, while `BaseResponse` and `GenericResponse[T]` model standard JSON responses with a typed body.
