# Item REST Tests (`test/item/rest`)

End-to-end tests hitting the REST API with `httptest.Server` and SQL mocks. Both success and failure scenarios are exercised. They run by default when no `grpc` or `kafka` build tags are set.
