# Proto Definitions (`proto`)

This directory stores the `.proto` files that define the gRPC services and messages used by the project.
The generated Go code lives in the `pb` package. Run `go generate` or invoke `protoc` whenever these files change to regenerate the bindings and documentation.
