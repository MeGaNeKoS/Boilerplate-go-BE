# Item Service Tests (`test/item`)

Holds end-to-end tests for the Item service. Subfolders correspond to transports: `rest`, `grpc` and `kafka`. Use the `grpc` or `kafka` build tags to enable those suites when running `go test`.
